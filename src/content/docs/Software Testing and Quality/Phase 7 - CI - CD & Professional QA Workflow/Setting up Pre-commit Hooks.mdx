---
title: Setting up Pre-commit Hooks
description: Use pre-commit to run formatting and linting before commits, keeping repos clean automatically.
sidebar:
  order: 238
---

## What pre-commit does

`pre-commit` runs checks before a commit is created.

This helps:

- keep formatting consistent
- avoid committing broken code

## Example .pre-commit-config.yaml

```yaml
repos:
  - repo: https://github.com/psf/black
    rev: 24.10.0
    hooks:
      - id: black

  - repo: https://github.com/pycqa/flake8
    rev: 7.1.1
    hooks:
      - id: flake8
```

## Install and run

```bash
pre-commit install
pre-commit run --all-files
```

## Tip

Still keep CI checks—hooks are helpful but not a replacement.
