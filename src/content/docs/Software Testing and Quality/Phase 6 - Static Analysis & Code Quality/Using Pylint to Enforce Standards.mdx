---
title: Using Pylint to Enforce Standards
description: Use pylint for deeper linting - code smells, design issues, naming conventions, and custom rules.
sidebar:
  order: 230
---

## What Pylint is good at

- naming conventions
- missing docstrings
- too many branches/arguments
- potential errors

## Example run

```bash
pylint your_package
```

## Typical config (pyproject.toml)

```toml
tool.pylint."MESSAGES CONTROL"
disable = ["C0114", "C0115", "C0116"]

[tool.pylint.format]
max-line-length = 88
```

## How to read Pylint output

- `C` convention
- `W` warning
- `E` error
- `R` refactor

## Tip

Don’t enable every rule at once in a legacy project.

Adopt gradually.
