---
title: Phase 6 - Unsupervised Learning
description: Learn to discover structure without labels - clustering, anomaly detection, and association rules.
sidebar:
  order: 290
---

## What unsupervised learning is

Unsupervised learning works with **features only (X)** — no labels `y`.

Goals include:

- **clustering**: group similar points
- **dimensionality reduction**: compress data
- **anomaly detection**: find rare/unusual samples
- **association rules**: discover co-occurrence patterns

```mermaid
flowchart LR
  X[Unlabeled data (X)] --> A[Unsupervised algorithm]
  A --> O[Structure: clusters / anomalies / rules]
```

## Phase 6 topics

1. Introduction to Clustering
2. K-Means Clustering Algorithm
3. The Elbow Method for Finding "K"
4. Hierarchical Clustering (Dendrograms)
5. DBSCAN: Density-Based Clustering
6. Anomaly Detection with Isolation Forests
7. Association Rule Learning (Apriori Algorithm)

## Common real-world uses

- customer segmentation
- grouping products by similarity
- detecting fraud/anomalies
- analyzing basket/cart patterns
