---
title: CRUD - Create Record
description: Insert rows into the database using db.session.add and commit transactions.
sidebar:
  order: 377
---

Creating records means:

- instantiate a model
- add it to the session
- commit

## Example

```python
from flask import Flask
from flask_sqlalchemy import SQLAlchemy

app = Flask(__name__)
app.config["SQLALCHEMY_DATABASE_URI"] = "sqlite:///app.db"

db = SQLAlchemy(app)


class User(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(80), unique=True, nullable=False)


with app.app_context():
    db.create_all()

    user = User(username="ravi")
    db.session.add(user)
    db.session.commit()

    print(user.id)  # id is available after commit
```

## Session basics

- `db.session` holds pending changes
- `commit()` finalizes the transaction

If something fails, you can:

```python
db.session.rollback()
```

## Common pitfalls

- Forgetting `commit()` (data doesn’t persist)
- Not handling `IntegrityError` (unique constraint violations)
- Doing heavy DB work inside request handlers without optimization
