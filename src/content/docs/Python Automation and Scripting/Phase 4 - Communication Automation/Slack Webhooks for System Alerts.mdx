---
title: Slack Webhooks for System Alerts
description: Send quick alerts to Slack via incoming webhooks.
sidebar:
  order: 175
---

## Incoming webhooks

Create an incoming webhook URL in your Slack workspace.

Store it as an environment variable:

- `SLACK_WEBHOOK_URL`

## Example

```python title="slack_webhook.py" showLineNumbers{1}
import os
import requests


def slack_alert(text: str):
    url = os.environ["SLACK_WEBHOOK_URL"]
    r = requests.post(url, json={"text": text}, timeout=10)
    r.raise_for_status()


# slack_alert("Job succeeded")
```
