---
title: Sending HTML Emails and Attachments
description: Build MIME emails with HTML bodies and attachments.
sidebar:
  order: 171
---

## HTML email + attachment

```python title="smtp_html_attachment.py" showLineNumbers{1}
import os
import smtplib
from email.message import EmailMessage
from pathlib import Path


def send_html_with_attachment(to: str, subject: str, html: str, attachment: Path):
    user = os.environ["SMTP_USER"]
    password = os.environ["SMTP_PASS"]

    msg = EmailMessage()
    msg["From"] = user
    msg["To"] = to
    msg["Subject"] = subject

    msg.set_content("Your email client does not support HTML.")
    msg.add_alternative(html, subtype="html")

    data = attachment.read_bytes()
    msg.add_attachment(
        data,
        maintype="application",
        subtype="octet-stream",
        filename=attachment.name,
    )

    with smtplib.SMTP("smtp.gmail.com", 587) as smtp:
        smtp.starttls()
        smtp.login(user, password)
        smtp.send_message(msg)


# send_html_with_attachment("to@example.com", "Report", "<h1>Hi</h1>", Path("report.pdf"))
```
