---
title: Automating Discord Messages
description: Send notifications to Discord via webhook URLs.
sidebar:
  order: 176
---

## Discord webhooks

Create a webhook in a Discord channel and store it as:

- `DISCORD_WEBHOOK_URL`

## Example

```python title="discord_webhook.py" showLineNumbers{1}
import os
import requests


def discord_send(content: str):
    url = os.environ["DISCORD_WEBHOOK_URL"]
    r = requests.post(url, json={"content": content}, timeout=10)
    r.raise_for_status()


# discord_send("Automation finished")
```
