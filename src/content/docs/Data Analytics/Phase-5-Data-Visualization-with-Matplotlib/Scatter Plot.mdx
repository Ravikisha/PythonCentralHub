---
title: Scatter Plot
description: Explore relationships between two numeric variables and handle overplotting and annotations.
sidebar:
  order: 5
---

## Basic scatter plot

```python title="Scatter" showLineNumbers{1}
import matplotlib.pyplot as plt

x = [1, 2, 3, 4, 5, 6, 7]
y = [50, 55, 65, 70, 75, 78, 85]

plt.figure(figsize=(7, 4))
plt.scatter(x, y)
plt.title("Hours vs score")
plt.xlabel("Hours")
plt.ylabel("Score")
plt.grid(True, alpha=0.3)
plt.tight_layout()
plt.show()
```

## Handle overplotting

Use transparency:

```python title="Alpha" showLineNumbers{1}
import matplotlib.pyplot as plt

plt.figure(figsize=(7, 4))
plt.scatter(x, y, alpha=0.6)
plt.title("Scatter with alpha")
plt.tight_layout()
plt.show()
```

## Annotate points

```python title="Annotate" showLineNumbers{1}
import matplotlib.pyplot as plt

plt.figure(figsize=(7, 4))
plt.scatter(x, y)

plt.annotate("top", xy=(7, 85), xytext=(6, 88), arrowprops={"arrowstyle": "->"})

plt.title("Annotated scatter")
plt.tight_layout()
plt.show()
```
