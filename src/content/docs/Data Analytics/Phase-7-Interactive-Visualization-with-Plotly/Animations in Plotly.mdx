---
title: Animations in Plotly
description: Animate Plotly charts over time (or a category) using animation frames.
sidebar:
  order: 12
---

## Why animations

Animations are useful when you want to show change over time:

- GDP vs life expectancy by year
- Covid cases by month

## Animated scatter (classic example)

```python title="Animated scatter" showLineNumbers{1}
import plotly.express as px

df = px.data.gapminder()

fig = px.scatter(
    df,
    x="gdpPercap",
    y="lifeExp",
    size="pop",
    color="continent",
    hover_name="country",
    log_x=True,
    size_max=60,
    animation_frame="year",
    animation_group="country",
    title="Gapminder: GDP vs Life Expectancy (animated)",
)

fig.show()
```

## Tips

- Animations work best with fewer points (or aggregation).
- Add `animation_group` so Plotly tracks each entity across frames.
- Consider facets if animation is too visually busy.
