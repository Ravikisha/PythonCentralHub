---
title: Interactive Histograms and Distributions
description: Build interactive histograms and distribution visualizations with Plotly Express.
sidebar:
  order: 10
---

## Why interactive distributions

Interactive distributions help you:

- zoom into interesting ranges
- inspect outliers
- compare groups with hover tooltips

## Histogram

```python title="Histogram" showLineNumbers{1}
import pandas as pd
import plotly.express as px

df = px.data.tips()

fig = px.histogram(
    df,
    x="total_bill",
    nbins=30,
    title="Total bill distribution",
)
fig.show()
```

## Histogram by category

```python title="Histogram by sex" showLineNumbers{1}
import plotly.express as px

df = px.data.tips()

fig = px.histogram(
    df,
    x="total_bill",
    color="sex",
    nbins=25,
    barmode="overlay",
    opacity=0.6,
    title="Total bill distribution by sex",
)
fig.show()
```

## Box plot (interactive)

```python title="Interactive box plot" showLineNumbers{1}
import plotly.express as px

df = px.data.tips()

fig = px.box(df, x="day", y="total_bill", color="sex", title="Total bill by day")
fig.show()
```

## Tip

Start with a histogram, then add:

- category color
- hover fields
- facet columns/rows
