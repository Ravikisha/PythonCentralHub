---
title: 3D Scatter Plots
description: Plot three numeric variables in 3D and understand when 3D helps (and when it doesn’t).
sidebar:
  order: 7
---

## Use 3D carefully

3D plots can look impressive but are often harder to interpret.

Use 3D when:

- You genuinely need three dimensions
- Interaction (rotate/zoom) helps understanding

## Example

```python title="3D Scatter" showLineNumbers{1}
import pandas as pd
import plotly.express as px

df = pd.DataFrame({
    "x": [1, 2, 3, 4, 5],
    "y": [10, 6, 8, 4, 7],
    "z": [100, 120, 90, 150, 110],
    "group": ["A", "A", "B", "B", "A"],
})

fig = px.scatter_3d(df, x="x", y="y", z="z", color="group", title="3D scatter")
fig.show()
```
