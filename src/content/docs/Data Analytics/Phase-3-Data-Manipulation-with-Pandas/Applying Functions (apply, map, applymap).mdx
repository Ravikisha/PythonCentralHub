---
title: Applying Functions (apply, map, applymap)
description: Transform values using vectorized operations, map, apply, and applymap, and learn when each is appropriate.
sidebar:
  order: 13
---

## First: prefer vectorized operations

Pandas is fastest when you use vectorized operations.

```python title="Vectorized example" showLineNumbers{1}
import pandas as pd

df = pd.DataFrame({"amount": [100, 200, 150]})
df["amount_with_tax"] = df["amount"] * 1.18
print(df)
```

Use `apply`/`map` when you really need custom logic.

## `map()` (Series only)

`map` transforms values in a **single Series**.

### Example: map categories

```python title="map categories" showLineNumbers{1}
import pandas as pd

s = pd.Series(["pune", "delhi", "mumbai"])
state_map = {"pune": "MH", "delhi": "DL", "mumbai": "MH"}

print(s.map(state_map))
```

If a value is missing from the map, it becomes `NaN`.

## `apply()` (Series or DataFrame)

### Apply on a Series

```python title="apply on Series" showLineNumbers{1}
import pandas as pd

s = pd.Series([1, 2, 3, 4])
print(s.apply(lambda x: x * x))
```

### Apply on rows (axis=1)

```python title="apply on rows" showLineNumbers{1}
import pandas as pd

df = pd.DataFrame({
    "name": ["Asha", "Ravi"],
    "score": [88, 91],
    "bonus": [5, 2],
})

def final_score(row):
    return row["score"] + row["bonus"]

df["final"] = df.apply(final_score, axis=1)
print(df)
```

Row-wise `apply` is easy to write but can be slower for large data.

## `applymap()` (DataFrame element-wise)

Element-wise transform for the whole DataFrame.

```python title="applymap" showLineNumbers{1}
import pandas as pd

df = pd.DataFrame({"a": [1, 2], "b": [3, 4]})
print(df.applymap(lambda x: x * 10))
```

## Quick guidance

- Use vectorized operations if possible
- Use `map` for simple value mapping (Series)
- Use `apply` for custom logic on a Series or across rows/columns
- Use `applymap` for element-wise transforms

import DataCampExercise from "../../../../components/DataCampExercise.astro";

## 🧪 Try It Yourself

### Exercise 1 – Create a DataFrame

<DataCampExercise
  lang="python"
  hint={`Pass a dict of lists to \`pd.DataFrame()\` to build a table.`}
  code={`# Task: Exercise 1 – Create a DataFrame
import pandas as pd

data = {"name": ["Alice", "Bob", "Charlie"], "score": [90, 85, 92]}
# Hint: use pd.DataFrame()
df = pd.___( data )    # replace ___ with DataFrame
print(df)

# ── Expected Output ───────────────────────────────────────────
#       name  score
# 0    Alice     90
# 1      Bob     85
# 2  Charlie     92
# ──────────────────────────────────────────────────────────────`}
  solution={`import pandas as pd

data = {"name": ["Alice", "Bob", "Charlie"], "score": [90, 85, 92]}
df = pd.DataFrame(data)
print(df)`}
  sct={`test_output_contains("Alice")
test_output_contains("score")
success_msg("DataFrame created!")`}
  height={130}
/>

### Exercise 2 – Select a Column

<DataCampExercise
  lang="python"
  hint={`Use \`df['column_name']\` to select a Series.`}
  code={`# Task: Exercise 2 – Select a Column
import pandas as pd

df = pd.DataFrame({"product": ["A", "B", "C"], "price": [10, 25, 15]})
# Hint: select the 'price' column
prices = df[___]         # replace ___ with 'price'
print(prices.mean())

# ── Expected Output ───────────────────────────────────────────
# 16.666666666666668
# ──────────────────────────────────────────────────────────────`}
  solution={`import pandas as pd

df = pd.DataFrame({"product": ["A", "B", "C"], "price": [10, 25, 15]})
prices = df["price"]
print(prices.mean())`}
  sct={`test_output_contains("16.6")
success_msg("Column selected and mean calculated!")`}
  height={130}
/>

### Exercise 3 – Filter Rows

<DataCampExercise
  lang="python"
  hint={`Use a boolean mask \`df[df['col'] > value]\` to filter rows.`}
  code={`# Task: Exercise 3 – Filter Rows
import pandas as pd

df = pd.DataFrame({"city": ["NY", "LA", "Chicago"], "pop": [8, 4, 3]})
# Hint: keep only rows where pop > 3
big = df[df[___] > ___]   # replace blanks: 'pop', 3
print(big)

# ── Expected Output ───────────────────────────────────────────
#   city  pop
# 0   NY    8
# 1   LA    4
# ──────────────────────────────────────────────────────────────`}
  solution={`import pandas as pd

df = pd.DataFrame({"city": ["NY", "LA", "Chicago"], "pop": [8, 4, 3]})
big = df[df["pop"] > 3]
print(big)`}
  sct={`test_output_contains("NY")
test_output_contains("LA")
success_msg("Row filter works!")`}
  height={130}
/>

