---
title: Handling Missing Data (isna, fillna, dropna)
description: Learn how Pandas represents missing values and how to detect, remove, and impute them safely.
sidebar:
  order: 7
---

## Why missing data is normal

Missing values happen because of:

- Optional form fields (e.g., phone number)
- Data entry errors
- Failed joins/merges
- Incomplete logs

Pandas typically represents missing values as `NaN` (and sometimes `None`).

## Setup example with missing values

```python title="Missing values example" showLineNumbers{1}
import pandas as pd
import numpy as np

df = pd.DataFrame({
    "name": ["Asha", "Ravi", "Meera", None],
    "age": [23, np.nan, 26, 31],
    "city": ["Pune", "Delhi", None, "Pune"],
    "score": [88, 91, np.nan, 95],
})

print(df)
```

## Detect missing values

### `isna()` / `isnull()`

```python title="isna" showLineNumbers{1}
print(df.isna())
```

### Count missing per column

```python title="Missing counts" showLineNumbers{1}
print(df.isna().sum())
```

## Remove missing values: `dropna()`

### Drop rows with any missing values

```python title="Drop rows with any NA" showLineNumbers{1}
clean = df.dropna()
print(clean)
```

### Drop rows where a specific column is missing

```python title="Drop rows where score is missing" showLineNumbers{1}
clean = df.dropna(subset=["score"])
print(clean)
```

## Fill missing values: `fillna()`

### Fill with a constant

```python title="Fill missing city" showLineNumbers{1}
filled = df.copy()
filled["city"] = filled["city"].fillna("Unknown")
print(filled)
```

### Fill numeric missing values with mean/median

```python title="Fill numeric with median" showLineNumbers{1}
filled = df.copy()
filled["score"] = filled["score"].fillna(filled["score"].median())
print(filled)
```

## Forward fill / backward fill

Useful for time series or repeated categories.

```python title="Forward fill" showLineNumbers{1}
filled = df.copy()
filled["city"] = filled["city"].ffill()
print(filled)
```

## Important: be explicit

Before deciding how to handle missing data, ask:

- Is missingness random or meaningful?
- Should missing values be removed or imputed?
- Will filling change the meaning of the data?

In analytics, documenting missing-data decisions is part of good practice.

import DataCampExercise from "../../components/DataCampExercise.astro";

## 🧪 Try It Yourself

### Exercise 1 – Create a DataFrame

<DataCampExercise
  lang="python"
  hint={`Pass a dict of lists to `pd.DataFrame()` to build a table.`}
  code={`# Task: Exercise 1 – Create a DataFrame
import pandas as pd

data = {"name": ["Alice", "Bob", "Charlie"], "score": [90, 85, 92]}
# Hint: use pd.DataFrame()
df = pd.___( data )    # replace ___ with DataFrame
print(df)

# ── Expected Output ───────────────────────────────────────────
#       name  score
# 0    Alice     90
# 1      Bob     85
# 2  Charlie     92
# ──────────────────────────────────────────────────────────────`}
  solution={`import pandas as pd

data = {"name": ["Alice", "Bob", "Charlie"], "score": [90, 85, 92]}
df = pd.DataFrame(data)
print(df)`}
  sct={`test_output_contains("Alice")
test_output_contains("score")
success_msg("DataFrame created!")`}
  height={130}
/>

### Exercise 2 – Select a Column

<DataCampExercise
  lang="python"
  hint={`Use `df['column_name']` to select a Series.`}
  code={`# Task: Exercise 2 – Select a Column
import pandas as pd

df = pd.DataFrame({"product": ["A", "B", "C"], "price": [10, 25, 15]})
# Hint: select the 'price' column
prices = df[___]         # replace ___ with 'price'
print(prices.mean())

# ── Expected Output ───────────────────────────────────────────
# 16.666666666666668
# ──────────────────────────────────────────────────────────────`}
  solution={`import pandas as pd

df = pd.DataFrame({"product": ["A", "B", "C"], "price": [10, 25, 15]})
prices = df["price"]
print(prices.mean())`}
  sct={`test_output_contains("16.6")
success_msg("Column selected and mean calculated!")`}
  height={130}
/>

### Exercise 3 – Filter Rows

<DataCampExercise
  lang="python"
  hint={`Use a boolean mask `df[df['col'] > value]` to filter rows.`}
  code={`# Task: Exercise 3 – Filter Rows
import pandas as pd

df = pd.DataFrame({"city": ["NY", "LA", "Chicago"], "pop": [8, 4, 3]})
# Hint: keep only rows where pop > 3
big = df[df[___] > ___]   # replace blanks: 'pop', 3
print(big)

# ── Expected Output ───────────────────────────────────────────
#   city  pop
# 0   NY    8
# 1   LA    4
# ──────────────────────────────────────────────────────────────`}
  solution={`import pandas as pd

df = pd.DataFrame({"city": ["NY", "LA", "Chicago"], "pop": [8, 4, 3]})
big = df[df["pop"] > 3]
print(big)`}
  sct={`test_output_contains("NY")
test_output_contains("LA")
success_msg("Row filter works!")`}
  height={130}
/>

