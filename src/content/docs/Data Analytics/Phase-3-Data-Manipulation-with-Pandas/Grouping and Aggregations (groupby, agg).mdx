---
title: Grouping and Aggregations (groupby, agg)
description: Use groupby to summarize data and compute metrics like count, mean, sum, and multiple aggregations.
sidebar:
  order: 9
---

## The idea

`groupby` answers questions like:

- Average revenue per city?
- Total orders per customer?
- Highest score per class?

Think: **split → apply → combine**.

## Example dataset

```python title="Sales sample" showLineNumbers{1}
import pandas as pd

sales = pd.DataFrame({
    "city": ["pune", "pune", "delhi", "delhi", "delhi"],
    "category": ["A", "B", "A", "A", "B"],
    "amount": [100, 200, 150, 120, 300],
})

print(sales)
```

## Basic aggregations

### Sum per city

```python title="Sum per city" showLineNumbers{1}
print(sales.groupby("city")["amount"].sum())
```

### Mean per city

```python title="Mean per city" showLineNumbers{1}
print(sales.groupby("city")["amount"].mean())
```

## Multiple aggregations with `agg`

```python title="Multiple metrics" showLineNumbers{1}
summary = (
    sales.groupby("city")
    .agg(
        total_amount=("amount", "sum"),
        avg_amount=("amount", "mean"),
        orders=("amount", "count"),
    )
)

print(summary)
```

## Group by multiple keys

```python title="Group by city and category" showLineNumbers{1}
summary = (
    sales.groupby(["city", "category"])
    .agg(total_amount=("amount", "sum"), orders=("amount", "count"))
    .reset_index()
)

print(summary)
```

## Common gotchas

- After `groupby`, the grouped columns become the index. Use `.reset_index()` if you want them as normal columns.
- Aggregation functions ignore missing numeric values by default (`NaN`) in many cases.

## Mental model

When you write:

```python
sales.groupby("city")["amount"].sum()
```

You are saying:

- Split the rows by `city`
- Select the `amount` column
- Sum within each group

This pattern comes up constantly in analytics.

import DataCampExercise from "../../components/DataCampExercise.astro";

## 🧪 Try It Yourself

### Exercise 1 – Create a DataFrame

<DataCampExercise
  lang="python"
  hint={`Pass a dict of lists to `pd.DataFrame()` to build a table.`}
  code={`# Task: Exercise 1 – Create a DataFrame
import pandas as pd

data = {"name": ["Alice", "Bob", "Charlie"], "score": [90, 85, 92]}
# Hint: use pd.DataFrame()
df = pd.___( data )    # replace ___ with DataFrame
print(df)

# ── Expected Output ───────────────────────────────────────────
#       name  score
# 0    Alice     90
# 1      Bob     85
# 2  Charlie     92
# ──────────────────────────────────────────────────────────────`}
  solution={`import pandas as pd

data = {"name": ["Alice", "Bob", "Charlie"], "score": [90, 85, 92]}
df = pd.DataFrame(data)
print(df)`}
  sct={`test_output_contains("Alice")
test_output_contains("score")
success_msg("DataFrame created!")`}
  height={130}
/>

### Exercise 2 – Select a Column

<DataCampExercise
  lang="python"
  hint={`Use `df['column_name']` to select a Series.`}
  code={`# Task: Exercise 2 – Select a Column
import pandas as pd

df = pd.DataFrame({"product": ["A", "B", "C"], "price": [10, 25, 15]})
# Hint: select the 'price' column
prices = df[___]         # replace ___ with 'price'
print(prices.mean())

# ── Expected Output ───────────────────────────────────────────
# 16.666666666666668
# ──────────────────────────────────────────────────────────────`}
  solution={`import pandas as pd

df = pd.DataFrame({"product": ["A", "B", "C"], "price": [10, 25, 15]})
prices = df["price"]
print(prices.mean())`}
  sct={`test_output_contains("16.6")
success_msg("Column selected and mean calculated!")`}
  height={130}
/>

### Exercise 3 – Filter Rows

<DataCampExercise
  lang="python"
  hint={`Use a boolean mask `df[df['col'] > value]` to filter rows.`}
  code={`# Task: Exercise 3 – Filter Rows
import pandas as pd

df = pd.DataFrame({"city": ["NY", "LA", "Chicago"], "pop": [8, 4, 3]})
# Hint: keep only rows where pop > 3
big = df[df[___] > ___]   # replace blanks: 'pop', 3
print(big)

# ── Expected Output ───────────────────────────────────────────
#   city  pop
# 0   NY    8
# 1   LA    4
# ──────────────────────────────────────────────────────────────`}
  solution={`import pandas as pd

df = pd.DataFrame({"city": ["NY", "LA", "Chicago"], "pop": [8, 4, 3]})
big = df[df["pop"] > 3]
print(big)`}
  sct={`test_output_contains("NY")
test_output_contains("LA")
success_msg("Row filter works!")`}
  height={130}
/>

