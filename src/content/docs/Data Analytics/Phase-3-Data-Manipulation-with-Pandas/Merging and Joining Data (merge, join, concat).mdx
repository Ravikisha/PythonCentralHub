---
title: Merging and Joining Data (merge, join, concat)
description: Combine multiple tables using merge/join (SQL-style) and stack data using concat.
sidebar:
  order: 10
---

## Why merges matter

Most real analytics uses multiple tables:

- Customers table
- Orders table
- Products table

You typically need to combine them to answer questions.

## Example tables

```python title="Customers and orders" showLineNumbers{1}
import pandas as pd

customers = pd.DataFrame({
    "customer_id": [1, 2, 3],
    "name": ["Asha", "Ravi", "Meera"],
})

orders = pd.DataFrame({
    "order_id": [101, 102, 103, 104],
    "customer_id": [1, 2, 2, 4],
    "amount": [250, 180, 90, 300],
})

print(customers)
print(orders)
```

## `merge`: SQL-style joins

### Inner join (only matching keys)

```python title="Inner join" showLineNumbers{1}
inner = customers.merge(orders, on="customer_id", how="inner")
print(inner)
```

### Left join (keep all left rows)

```python title="Left join" showLineNumbers{1}
left = customers.merge(orders, on="customer_id", how="left")
print(left)
```

### Outer join (keep everything)

```python title="Outer join" showLineNumbers{1}
outer = customers.merge(orders, on="customer_id", how="outer", indicator=True)
print(outer)
```

`indicator=True` adds a `_merge` column so you can see where each row came from.

## `join`: align by index

`join` is convenient when keys are indices.

```python title="join by index" showLineNumbers{1}
customers_idx = customers.set_index("customer_id")
orders_idx = orders.set_index("customer_id")

joined = customers_idx.join(orders_idx, how="left")
print(joined)
```

## `concat`: stack DataFrames

### Append rows (same columns)

```python title="concat rows" showLineNumbers{1}
jan = pd.DataFrame({"customer_id": [1, 2], "amount": [100, 200]})
feb = pd.DataFrame({"customer_id": [2, 3], "amount": [150, 50]})

all_orders = pd.concat([jan, feb], ignore_index=True)
print(all_orders)
```

### Add columns side-by-side

```python title="concat columns" showLineNumbers{1}
df1 = pd.DataFrame({"A": [1, 2]})
df2 = pd.DataFrame({"B": [10, 20]})

combined = pd.concat([df1, df2], axis=1)
print(combined)
```

## Merge tips

- Use `validate=` to enforce expected relationships (e.g., one-to-many).
- Watch for duplicate column names; use `suffixes=("_left", "_right")` when needed.
- After a left join, missing matches become `NaN`—handle them deliberately.

import DataCampExercise from "../../components/DataCampExercise.astro";

## 🧪 Try It Yourself

### Exercise 1 – Create a DataFrame

<DataCampExercise
  lang="python"
  hint={`Pass a dict of lists to `pd.DataFrame()` to build a table.`}
  code={`# Task: Exercise 1 – Create a DataFrame
import pandas as pd

data = {"name": ["Alice", "Bob", "Charlie"], "score": [90, 85, 92]}
# Hint: use pd.DataFrame()
df = pd.___( data )    # replace ___ with DataFrame
print(df)

# ── Expected Output ───────────────────────────────────────────
#       name  score
# 0    Alice     90
# 1      Bob     85
# 2  Charlie     92
# ──────────────────────────────────────────────────────────────`}
  solution={`import pandas as pd

data = {"name": ["Alice", "Bob", "Charlie"], "score": [90, 85, 92]}
df = pd.DataFrame(data)
print(df)`}
  sct={`test_output_contains("Alice")
test_output_contains("score")
success_msg("DataFrame created!")`}
  height={130}
/>

### Exercise 2 – Select a Column

<DataCampExercise
  lang="python"
  hint={`Use `df['column_name']` to select a Series.`}
  code={`# Task: Exercise 2 – Select a Column
import pandas as pd

df = pd.DataFrame({"product": ["A", "B", "C"], "price": [10, 25, 15]})
# Hint: select the 'price' column
prices = df[___]         # replace ___ with 'price'
print(prices.mean())

# ── Expected Output ───────────────────────────────────────────
# 16.666666666666668
# ──────────────────────────────────────────────────────────────`}
  solution={`import pandas as pd

df = pd.DataFrame({"product": ["A", "B", "C"], "price": [10, 25, 15]})
prices = df["price"]
print(prices.mean())`}
  sct={`test_output_contains("16.6")
success_msg("Column selected and mean calculated!")`}
  height={130}
/>

### Exercise 3 – Filter Rows

<DataCampExercise
  lang="python"
  hint={`Use a boolean mask `df[df['col'] > value]` to filter rows.`}
  code={`# Task: Exercise 3 – Filter Rows
import pandas as pd

df = pd.DataFrame({"city": ["NY", "LA", "Chicago"], "pop": [8, 4, 3]})
# Hint: keep only rows where pop > 3
big = df[df[___] > ___]   # replace blanks: 'pop', 3
print(big)

# ── Expected Output ───────────────────────────────────────────
#   city  pop
# 0   NY    8
# 1   LA    4
# ──────────────────────────────────────────────────────────────`}
  solution={`import pandas as pd

df = pd.DataFrame({"city": ["NY", "LA", "Chicago"], "pop": [8, 4, 3]})
big = df[df["pop"] > 3]
print(big)`}
  sct={`test_output_contains("NY")
test_output_contains("LA")
success_msg("Row filter works!")`}
  height={130}
/>

