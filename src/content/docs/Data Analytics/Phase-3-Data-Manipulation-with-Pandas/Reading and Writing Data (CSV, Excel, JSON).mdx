---
title: Reading and Writing Data (CSV, Excel, JSON)
description: Load datasets into Pandas from common file formats and export cleaned results.
sidebar:
  order: 3
---

## Reading a CSV file

CSV is the most common format in analytics.

```python title="Read CSV" showLineNumbers{1}
import pandas as pd

df = pd.read_csv("data/sales.csv")
print(df.head())
```

### Useful `read_csv()` options

```python title="read_csv options" showLineNumbers{1}
import pandas as pd

df = pd.read_csv(
    "data/sales.csv",
    sep=",",              # delimiter
    encoding="utf-8",     # encoding
    na_values=["NA", "", "null"],
)

print(df.info())
```

## Writing a CSV file

```python title="Write to CSV" showLineNumbers{1}
import pandas as pd

df = pd.DataFrame({"a": [1, 2], "b": [3, 4]})

df.to_csv("output/cleaned.csv", index=False)
```

## Reading Excel files

Excel needs an engine (often `openpyxl`).

```python title="Read Excel" showLineNumbers{1}
import pandas as pd

df = pd.read_excel("data/sales.xlsx", sheet_name="Sheet1")
print(df.head())
```

### Writing Excel

```python title="Write Excel" showLineNumbers{1}
import pandas as pd

df = pd.DataFrame({"a": [1, 2], "b": [3, 4]})

df.to_excel("output/cleaned.xlsx", index=False)
```

## Reading and writing JSON

### Reading JSON

```python title="Read JSON" showLineNumbers{1}
import pandas as pd

df = pd.read_json("data/users.json")
print(df.head())
```

### Writing JSON

```python title="Write JSON" showLineNumbers{1}
import pandas as pd

df = pd.DataFrame({"user": ["a", "b"], "score": [10, 20]})

df.to_json("output/users.json", orient="records", indent=2)
```

## Practical checklist for file IO

- Always confirm shape: `df.shape`
- Inspect columns: `df.columns`
- Preview: `df.head()` and `df.sample(5)`
- Validate types: `df.dtypes`
- Watch for missing values: `df.isna().sum()`

import DataCampExercise from "../../components/DataCampExercise.astro";

## 🧪 Try It Yourself

### Exercise 1 – Create a DataFrame

<DataCampExercise
  lang="python"
  hint={`Pass a dict of lists to `pd.DataFrame()` to build a table.`}
  code={`# Task: Exercise 1 – Create a DataFrame
import pandas as pd

data = {"name": ["Alice", "Bob", "Charlie"], "score": [90, 85, 92]}
# Hint: use pd.DataFrame()
df = pd.___( data )    # replace ___ with DataFrame
print(df)

# ── Expected Output ───────────────────────────────────────────
#       name  score
# 0    Alice     90
# 1      Bob     85
# 2  Charlie     92
# ──────────────────────────────────────────────────────────────`}
  solution={`import pandas as pd

data = {"name": ["Alice", "Bob", "Charlie"], "score": [90, 85, 92]}
df = pd.DataFrame(data)
print(df)`}
  sct={`test_output_contains("Alice")
test_output_contains("score")
success_msg("DataFrame created!")`}
  height={130}
/>

### Exercise 2 – Select a Column

<DataCampExercise
  lang="python"
  hint={`Use `df['column_name']` to select a Series.`}
  code={`# Task: Exercise 2 – Select a Column
import pandas as pd

df = pd.DataFrame({"product": ["A", "B", "C"], "price": [10, 25, 15]})
# Hint: select the 'price' column
prices = df[___]         # replace ___ with 'price'
print(prices.mean())

# ── Expected Output ───────────────────────────────────────────
# 16.666666666666668
# ──────────────────────────────────────────────────────────────`}
  solution={`import pandas as pd

df = pd.DataFrame({"product": ["A", "B", "C"], "price": [10, 25, 15]})
prices = df["price"]
print(prices.mean())`}
  sct={`test_output_contains("16.6")
success_msg("Column selected and mean calculated!")`}
  height={130}
/>

### Exercise 3 – Filter Rows

<DataCampExercise
  lang="python"
  hint={`Use a boolean mask `df[df['col'] > value]` to filter rows.`}
  code={`# Task: Exercise 3 – Filter Rows
import pandas as pd

df = pd.DataFrame({"city": ["NY", "LA", "Chicago"], "pop": [8, 4, 3]})
# Hint: keep only rows where pop > 3
big = df[df[___] > ___]   # replace blanks: 'pop', 3
print(big)

# ── Expected Output ───────────────────────────────────────────
#   city  pop
# 0   NY    8
# 1   LA    4
# ──────────────────────────────────────────────────────────────`}
  solution={`import pandas as pd

df = pd.DataFrame({"city": ["NY", "LA", "Chicago"], "pop": [8, 4, 3]})
big = df[df["pop"] > 3]
print(big)`}
  sct={`test_output_contains("NY")
test_output_contains("LA")
success_msg("Row filter works!")`}
  height={130}
/>

