---
title: Condition Variables
description: Use threading.Condition for advanced coordination (wait/notify) when a state changes.
sidebar:
  order: 119
---

## What is a Condition?

A `Condition` is used when threads need to:

- wait until a condition becomes true (e.g., “buffer not empty”)
- notify others when the state changes

It combines:

- a lock
- a wait/notify mechanism

## Producer-consumer with Condition

```python title="condition_example.py" showLineNumbers{1}
import threading
import time

items = []
cond = threading.Condition()


def producer():
    for i in range(5):
        time.sleep(0.2)
        with cond:
            items.append(i)
            print("produced", i)
            cond.notify()  # wake one waiting consumer


def consumer():
    for _ in range(5):
        with cond:
            while not items:
                cond.wait()  # release lock and wait
            v = items.pop(0)
        print("consumed", v)


p = threading.Thread(target=producer)
c = threading.Thread(target=consumer)

p.start(); c.start()
p.join(); c.join()
```

## Why the while loop?

Always re-check the condition after waking:

- spurious wakeups can happen
- another consumer might have consumed the item first

import DataCampExercise from "../../../../components/DataCampExercise.astro";

## 🧪 Try It Yourself

### Exercise 1 – Condition notify and wait

<DataCampExercise
  lang="python"
  hint={`Use \`with condition:\` then \`condition.wait()\` and \`condition.notify()\`.`}
  code={`# Task: Exercise 1 – Condition notify and wait
import threading

cond = threading.Condition()
items = []

def producer():
    with cond:
        items.append("data")
        cond.___()         # replace ___ with notify

def consumer():
    with cond:
        cond.___()         # replace ___ with wait
        print("Got:", items[0])

t1 = threading.Thread(target=consumer)
t2 = threading.Thread(target=producer)
t1.start(); t2.start()
t1.join(); t2.join()`}
  solution={`import threading

cond = threading.Condition()
items = []

def producer():
    with cond:
        items.append("data")
        cond.notify()

def consumer():
    with cond:
        cond.wait()
        print("Got:", items[0])

t1 = threading.Thread(target=consumer)
t2 = threading.Thread(target=producer)
t1.start(); t2.start()
t1.join(); t2.join()`}
  sct={`test_output_contains("Got: data")
success_msg("Condition variable used correctly!")`}
  height={160}
/>

### Exercise 2 – notify_all

<DataCampExercise
  lang="python"
  hint={`Use \`condition.notify_all()\` to wake up all waiting threads at once.`}
  code={`# Task: Exercise 2 – notify_all
import threading

cond = threading.Condition()
ready = []

def waiter(n):
    with cond:
        cond.wait()
        ready.append(n)

def broadcaster():
    with cond:
        cond.___()       # replace ___ with notify_all

threads = [threading.Thread(target=waiter, args=(i,)) for i in range(3)]
for t in threads: t.start()
broadcaster_t = threading.Thread(target=broadcaster)
broadcaster_t.start()
broadcaster_t.join()
for t in threads: t.join()
print("Ready count:", len(ready))`}
  solution={`import threading

cond = threading.Condition()
ready = []

def waiter(n):
    with cond:
        cond.wait()
        ready.append(n)

def broadcaster():
    with cond:
        cond.notify_all()

threads = [threading.Thread(target=waiter, args=(i,)) for i in range(3)]
for t in threads: t.start()
broadcaster_t = threading.Thread(target=broadcaster)
broadcaster_t.start()
broadcaster_t.join()
for t in threads: t.join()
print("Ready count:", len(ready))`}
  sct={`test_output_contains("Ready count: 3")
success_msg("notify_all wakes every waiting thread!")`}
  height={165}
/>

### Exercise 3 – Predicate Wait

<DataCampExercise
  lang="python"
  hint={`Use \`condition.wait_for(lambda: condition_expr)\` to avoid spurious wakeups.`}
  code={`# Task: Exercise 3 – Predicate Wait
import threading

cond = threading.Condition()
flag = False

def setter():
    global flag
    with cond:
        flag = True
        cond.notify()

def checker():
    with cond:
        # Hint: wait until flag is True
        cond.wait_for(lambda: ___)  # replace ___ with flag
        print("Flag is now True!")

t1 = threading.Thread(target=checker)
t2 = threading.Thread(target=setter)
t1.start(); t2.start()
t1.join(); t2.join()`}
  solution={`import threading

cond = threading.Condition()
flag = False

def setter():
    global flag
    with cond:
        flag = True
        cond.notify()

def checker():
    with cond:
        cond.wait_for(lambda: flag)
        print("Flag is now True!")

t1 = threading.Thread(target=checker)
t2 = threading.Thread(target=setter)
t1.start(); t2.start()
t1.join(); t2.join()`}
  sct={`test_output_contains("Flag is now True!")
success_msg("wait_for checks the predicate!")`}
  height={162}
/>

