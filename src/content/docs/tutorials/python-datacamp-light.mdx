---
title: DataCamp Light (Python) – Demo
description: A demo page for a DataCamp Light Python exercise (as per official snippet).
sidebar:
  order: 98
---

import DataCampExercise from "../../../components/DataCampExercise.astro";

## Isolated (iframe) embed

If the buttons are missing here but appear in the non-iframe version below, the issue is caused by iframe isolation/sandboxing.

<DataCampExercise
  lang="python"
  height={800}
  debug={true}
  hintPlacement="after"
  preExerciseCode={`# no pec`}
  code={`# Create a variable a, equal to 5


# Print out a`}
  solution={`# Create a variable a, equal to 5
a = 5

# Print out a
print(a)`}
  sct={`test_object("a")
test_function("print")
success_msg("Great job!")`}
  hint={`Use the assignment operator (=) to create the variable a.`}
/>

