---
title: Errors and Exceptions in Python
description: Learn Python errors vs exceptions, common exception types, and how to read tracebacks.
sidebar:
  order: 98
---

## What are errors and exceptions?

When Python can’t execute your code, it stops and reports a problem.

- **Syntax errors** happen when Python can’t even *parse* the code.
- **Exceptions** happen while the program is running (runtime problems).

## Syntax Error

A syntax error occurs when Python sees invalid code structure.

```python title="syntax_error.py" showLineNumbers{1}
# Missing colon
if 10 > 5
    print("10 is greater")
```

Typical output:

```text title="Traceback" showLineNumbers{1}
  File "syntax_error.py", line 2
    if 10 > 5
             ^
SyntaxError: expected ':'
```

## Exception (runtime error)

Example: division by zero.

```python title="zero_division.py" showLineNumbers{1}
print(10 / 0)
```

```text title="Traceback" showLineNumbers{1}
Traceback (most recent call last):
  File "zero_division.py", line 1, in <module>
    print(10 / 0)
ZeroDivisionError: division by zero
```

## How to read a traceback

A traceback tells you:

1. **Where** the error occurred (file and line number)
2. The **call stack** (which functions were called)
3. The **exception type** and message

## Common exception types

- `NameError` – variable not defined
- `TypeError` – wrong type used (e.g., add str + int)
- `ValueError` – correct type, invalid value (e.g., int("abc"))
- `IndexError` – list index out of range
- `KeyError` – dict key missing
- `FileNotFoundError` – file path not found

```python title="common_exceptions.py" showLineNumbers{1}
# NameError
# print(x)

# TypeError
# print("age: " + 10)

# ValueError
# print(int("abc"))

# IndexError
# nums = [1, 2]
# print(nums[10])

# KeyError
# d = {"a": 1}
# print(d["missing"])

# FileNotFoundError
# open("missing.txt", "r")
```

## Next

In the next pages you’ll learn:

- Handling exceptions with `try/except`
- Cleaning up resources with `finally` and context managers
- Creating custom exceptions

import DataCampExercise from "../../components/DataCampExercise.astro";

## 🧪 Try It Yourself

### Exercise 1 – Catch a ValueError

<DataCampExercise
  lang="python"
  hint={`Wrap the int() call in a try/except ValueError block.`}
  code={`# Task: Exercise 1 – Catch a ValueError
# Hint: use try/except to handle the error gracefully
___:                              # replace ___ with try
    num = int("not_a_number")
    print("Converted:", num)
___ ValueError:                   # replace ___ with except
    print("Caught a ValueError!")

# ── Expected Output ───────────────────────────────────────────
# Caught a ValueError!
# ──────────────────────────────────────────────────────────────`}
  solution={`try:
    num = int("not_a_number")
    print("Converted:", num)
except ValueError:
    print("Caught a ValueError!")`}
  sct={`test_output_contains("Caught a ValueError!")
success_msg("ValueError caught successfully!")`}
  height={130}
/>

### Exercise 2 – Multiple Except Blocks

<DataCampExercise
  lang="python"
  hint={`Add a separate `except ZeroDivisionError` clause.`}
  code={`# Task: Exercise 2 – Multiple Except Blocks
def safe_divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        return "Cannot divide by zero"
    except ___:                 # replace ___ with TypeError
        return "Invalid types"

print(safe_divide(10, 0))
print(safe_divide(10, "x"))

# ── Expected Output ───────────────────────────────────────────
# Cannot divide by zero
# Invalid types
# ──────────────────────────────────────────────────────────────`}
  solution={`def safe_divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        return "Cannot divide by zero"
    except TypeError:
        return "Invalid types"

print(safe_divide(10, 0))
print(safe_divide(10, "x"))`}
  sct={`test_output_contains("Cannot divide by zero")
test_output_contains("Invalid types")
success_msg("Multiple except blocks work!")`}
  height={150}
/>

### Exercise 3 – Raise a Custom Exception

<DataCampExercise
  lang="python"
  hint={`Use `raise ValueError(message)` inside an if block.`}
  code={`# Task: Exercise 3 – Raise a Custom Exception
def check_age(age):
    if age < 0:
        # Hint: raise a ValueError with a message
        ___ ValueError("Age cannot be negative")  # replace ___ with raise
    return f"Age is {age}"

try:
    print(check_age(-5))
except ValueError as e:
    print("Error:", e)

# ── Expected Output ───────────────────────────────────────────
# Error: Age cannot be negative
# ──────────────────────────────────────────────────────────────`}
  solution={`def check_age(age):
    if age < 0:
        raise ValueError("Age cannot be negative")
    return f"Age is {age}"

try:
    print(check_age(-5))
except ValueError as e:
    print("Error:", e)`}
  sct={`test_output_contains("Error: Age cannot be negative")
success_msg("raise creates custom errors!")`}
  height={150}
/>

